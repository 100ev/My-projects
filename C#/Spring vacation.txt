int days = int.Parse(Console.ReadLine());
            double budget = double.Parse(Console.ReadLine());
            int numPeople = int.Parse(Console.ReadLine());
            double fuelPerKm = double.Parse(Console.ReadLine());
            double foodExpense = double.Parse(Console.ReadLine());
            double roomPrice = double.Parse(Console.ReadLine());

            double totFoodExpenses = 0.0;
            double totHotelPrice = 0.0;
            double totExpense = 0.0;
            double sum = 0.0;
            double consFuel = 0.0;
            int traveledDis = 0;
            double groupDiscount = 0.0;
            double totFuelExpenses = 0.0;
            double finalSum = 0.0;

            for (int curDay = 0; curDay < days; curDay++)
            {

                int travDistPerDay = int.Parse(Console.ReadLine());
                traveledDis += travDistPerDay;

                totFoodExpenses = (foodExpense * numPeople) * days;
                totHotelPrice = (roomPrice * numPeople) * days;
                totExpense = totFoodExpenses + totHotelPrice;

                consFuel = traveledDis * fuelPerKm;
                if (numPeople > 10)
                {
                    
                }

                
                if (curDay % 3 == 0 && curDay % 5 == 0)
                {
                    totFuelExpenses += traveledDis * 0.4;
                    sum = totExpense + totFuelExpenses;
                }
                if (curDay % 7 == 0)
                {
                    double recievedMoney = totExpense / numPeople;
                    sum += totExpense + recievedMoney;           
                }

                finalSum = sum + totExpense;
            }
            Console.WriteLine(finalSum);